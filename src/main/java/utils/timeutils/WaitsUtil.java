package utils.timeutils;


/*import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.FluentWait;
import org.openqa.selenium.support.ui.Wait;

import java.time.Duration;

public class WaitsUtil {
    public static WebDriver webDriver;
    public static Wait<WebDriver> smartWait;
    public static Duration timeOut;

    public WaitsUtil(UIDriver uiDriver, long time) {
        webDriver = uiDriver.getWebDriver();
        timeOut = Duration.ofSeconds(time);

        smartWait = new FluentWait<WebDriver>(webDriver)
                .withTimeout(timeOut)
                .pollingEvery(Duration.ofSeconds(5L))
                .ignoring(Exception.class);
    }
}*/
